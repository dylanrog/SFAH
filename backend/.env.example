# Copy to backend/.env and adjust.
DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/mise
# "private" serves all corpora locally; "public" (deployed demo) serves only the public corpus.
APP_MODE=private
